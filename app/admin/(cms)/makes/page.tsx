import Link from "next/link";
import { createMakeAction } from "@/app/admin/actions";
import { getAllMakes } from "@/lib/cms-data";

export default async function MakesAdminPage() { const makes = await getAllMakes(); return <div className="mx-auto max-w-5xl space-y-7"><header className="flex flex-wrap items-end justify-between gap-4"><div><p className="admin-eyebrow">Content</p><h1 className="admin-title">Makes</h1></div><form action={createMakeAction}><button className="admin-button">New Make</button></form></header><div className="space-y-3">{makes.map((make) => <Link key={make.id} href={`/admin/makes/${make.id}`} className="admin-panel flex items-center justify-between gap-4 hover:border-olive-700/30"><div><h2 className="font-serif text-2xl text-olive-900">{make.title}</h2><p className="mt-1 text-sm text-charcoal-700">/{make.slug} · {make.craft_type || "Craft not set"}</p></div><span className={make.status === "published" ? "admin-status-published" : "admin-status-draft"}>{make.status}</span></Link>)}{!makes.length ? <p className="rounded-2xl border border-dashed border-olive-900/20 p-10 text-center text-charcoal-700">No makes yet. Start with your first project.</p> : null}</div></div>; }
