import { ArchiveEmptyState } from "@/components/archive-empty-state";
import { MakeCard } from "@/components/make-card";
import { getPublishedMakes } from "@/lib/cms-data";

export default async function MakesPage() { const makes = await getPublishedMakes(); return <main className="mx-auto w-full max-w-7xl px-6 py-14 sm:px-10 lg:px-12 lg:py-20"><header className="max-w-3xl"><p className="stitch-label text-pimento-700">Made by hand</p><h1 className="mt-4 font-serif text-6xl tracking-[-0.035em] text-olive-900 sm:text-7xl">Makes</h1><p className="mt-5 max-w-2xl text-xl leading-8 text-charcoal-700">Finished pieces and the stories, materials, and lessons held inside them.</p></header>{makes.length ? <div className="mt-12 grid gap-x-6 gap-y-12 sm:grid-cols-2 lg:grid-cols-3">{makes.map((make, index) => <MakeCard key={make.id} make={make} priority={index < 3} />)}</div> : <ArchiveEmptyState label="The first make is still being gathered." motif="yarn" className="bright-textile-placeholder" />}</main>; }
